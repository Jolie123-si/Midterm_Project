package Music_Library_system.Music_Library_system.models;

/**
 * Enum for Location Types in Rwanda
 * Hierarchical structure: PROVINCE → DISTRICT → SECTOR → CELL
 */
public enum ELocationType {
    PROVINCE,
    DISTRICT,
    SECTOR,
    CELL
}
